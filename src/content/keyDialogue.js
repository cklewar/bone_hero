const english = [
  [
    "No entrance without the magic key of the magician!",
    "You need to grab the key placed somewhere in the graveyard.",
  ],
];

const keyLines = {
  english,
};

export default keyLines;
